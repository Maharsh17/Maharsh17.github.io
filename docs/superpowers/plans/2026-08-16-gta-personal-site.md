# GTA San Andreas Personal Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the combined GTA San Andreas web recreation into Maharsh Jani's personal website, using the save-file screen as the organizing metaphor, with every number traceable to a real source.

**Architecture:** Static multi-page site. Each page is its own HTML document loading only its own stylesheet, because `gtasamenu.css` and `gtasa-page.css` define `.container` incompatibly and one document per project is what keeps the recreations pixel-identical. Content comes from two JSON files: one generated by a `gh api` call, one hand-authored. No build system, no server, no framework.

**Tech Stack:** HTML, CSS, vanilla JavaScript (ES5-compatible, no bundler), `gh` CLI + `jq` for data generation, `python3 -m http.server` for local serving, `node --check` for syntax verification.

**Spec:** `docs/superpowers/specs/2026-08-16-gta-personal-site-design.md`

## Global Constraints

Every task's requirements implicitly include these.

- **No em dashes** in any user-visible copy. Use a plain dash.
- **No invented numbers.** Every figure on the rendered site traces to `data/projects.json` or the resume PDF. If a number cannot be derived, use a label instead.
- **No assets from the retail install** at `gta-web/GTA San Andreas/`. Reference only.
- **Phone number is omitted** from all pages. Email and LinkedIn only.
- **One stylesheet per document.** Never load `gtasamenu.css` and `gtasa-page.css` in the same page.
- **Never edit files under `assets/`.** Those are vendored recreations. New styling goes in `css/site.css`.
- **ES5-compatible JavaScript.** No bundler exists, so no imports, no optional chaining, no arrow functions in shipped files.
- Working directory for all commands: `/Users/maharsh/Documents/Research/gta-web`.

## File Structure

| File | Responsibility |
|---|---|
| `check.sh` | The whole verification harness. Serves, checks status codes, audits asset paths, syntax-checks JS. |
| `scripts/gen-projects.sh` | Single `gh api` call producing `data/projects.json`. Never hand-edited output. |
| `combined-gta/data/projects.json` | Generated. Repo facts: name, date, languages, archived, release count. |
| `combined-gta/data/overrides.json` | Hand-authored. Slot names, blurbs, status, ordering. Generator never touches it. |
| `combined-gta/js/slots.js` | Renders save slots into `loadgame.html`. |
| `combined-gta/js/stats.js` | Renders skill bars into `stats.html`. |
| `combined-gta/js/site.js` | Existing. Site-wide notification layer. |
| `combined-gta/css/site.css` | The only new stylesheet. Namespaced under `.site-*`. |

---

### Task 1: Repository and Verification Harness

Nothing else can be verified until this exists, so it comes first. `gta-web/` is not currently a git repository.

**Files:**
- Create: `.gitignore`
- Create: `check.sh`

**Interfaces:**
- Consumes: nothing
- Produces: `./check.sh` exits 0 when the site is healthy, non-zero otherwise. Every later task ends by running it.

- [ ] **Step 1: Initialize the repository**

The clones and the 4.8 GB game install must never be committed.

```bash
cd /Users/maharsh/Documents/Research/gta-web
git init
```

- [ ] **Step 2: Write `.gitignore`**

```
GTA San Andreas/
gta-sa-menu/
gta-sa-stats/
gta-sa-notification-js/
gta-sa-hud-react/
.DS_Store
```

- [ ] **Step 3: Write the failing verification harness**

Create `check.sh`:

```bash
#!/usr/bin/env bash
# Verification harness for combined-gta. Exits non-zero on any failure.
set -uo pipefail
cd "$(dirname "$0")/combined-gta" || exit 1

PORT=8899
FAIL=0
fail() { echo "FAIL: $*"; FAIL=1; }

PAGES="index.html loadgame.html stats.html missions.html map.html hud.html options.html quitgame.html"

# 1. Required pages exist
for p in $PAGES; do
  [ -f "$p" ] || fail "missing page $p"
done

# 2. Retired pages are gone
for p in game.html deletegame.html language.html notifications.html; do
  [ -e "$p" ] && fail "retired page still present: $p"
done

# 3. JavaScript syntax
for j in js/*.js; do
  node --check "$j" >/dev/null 2>&1 || fail "syntax error in $j"
done

# 4. Data files are valid JSON
for d in data/projects.json data/overrides.json; do
  [ -f "$d" ] || { fail "missing $d"; continue; }
  jq empty "$d" >/dev/null 2>&1 || fail "invalid JSON in $d"
done

# 5. Every local href/src resolves
for f in *.html; do
  grep -oE '(href|src)="\./[^"]*"' "$f" | sed -E 's/^(href|src)="//; s/"$//' | sort -u | while read -r p; do
    [ -e "$p" ] || echo "FAIL: $f references missing $p"
  done
done | grep FAIL && FAIL=1

# 6. Every CSS url() resolves
for c in $(find assets css -name '*.css' 2>/dev/null); do
  d=$(dirname "$c")
  grep -oE 'url\(["'"'"']?[^)"'"'"']+' "$c" | sed -E 's/url\(["'"'"']?//' \
    | grep -v '^data:' | sed 's/[?#].*//' | sort -u | while read -r u; do
      [ -e "$d/$u" ] || echo "FAIL: $c references missing $u"
    done
done | grep FAIL && FAIL=1

# 7. Pages serve 200
lsof -ti :$PORT | xargs kill -9 2>/dev/null
python3 -m http.server $PORT >/dev/null 2>&1 &
SRV=$!
sleep 2
for p in $PAGES; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "http://localhost:$PORT/$p")
  [ "$code" = "200" ] || fail "$p returned $code"
done
kill $SRV 2>/dev/null
wait $SRV 2>/dev/null

[ $FAIL -eq 0 ] && echo "ALL CHECKS PASSED"
exit $FAIL
```

- [ ] **Step 4: Make it executable and run it to verify it FAILS**

```bash
chmod +x check.sh
./check.sh
```

Expected: FAIL lines for `missing page missions.html`, `retired page still present: game.html`, `missing data/projects.json`. This is correct. The harness describes the target state, and nothing has been built yet.

- [ ] **Step 5: Commit**

```bash
git add .gitignore check.sh
git commit -m "Add verification harness and gitignore"
```

---

### Task 2: Data Generation

**Files:**
- Create: `scripts/gen-projects.sh`
- Create: `combined-gta/data/projects.json` (generated)
- Create: `combined-gta/data/overrides.json` (hand-authored)

**Interfaces:**
- Consumes: nothing
- Produces: `data/projects.json` with shape `{generated: string, repos: [{nameWithOwner, pushedAt, isPrivate, isArchived, description, releaseCount, languages: [{name, size}]}]}`. `data/overrides.json` keyed by `nameWithOwner`, each value `{slot, slotName, blurb, status}`. `slot` is 1-8. Task 3 and Task 4 both read these exact field names.

- [ ] **Step 1: Write the generator**

Create `scripts/gen-projects.sh`. The GraphQL query and jq filter below are verified working.

```bash
#!/usr/bin/env bash
# Regenerates combined-gta/data/projects.json from the GitHub API.
# Safe to rerun. Never edit the output by hand; edit overrides.json instead.
set -euo pipefail
cd "$(dirname "$0")/.."

REPOS='["InviteInstitute/lm-dashboard","InviteInstitute/vex-agent-integration","InviteInstitute/agent-lm-packages","Maharsh17/county-clustering","CS222-UIUC/fa25-team118-titans","Maharsh17/resume-blaster","HarshithaS2023/ai4all"]'

gh api graphql -f query='
{
  viewer {
    repositories(first:100, ownerAffiliations:[OWNER,COLLABORATOR,ORGANIZATION_MEMBER]) {
      nodes {
        nameWithOwner isPrivate isArchived pushedAt description
        releases { totalCount }
        languages(first:8, orderBy:{field:SIZE,direction:DESC}) {
          edges { size node { name } }
        }
      }
    }
  }
}' | jq --argjson want "$REPOS" '{
  generated: (now | strftime("%Y-%m-%d")),
  repos: [
    .data.viewer.repositories.nodes[]
    | select(.nameWithOwner as $n | $want | index($n))
    | {
        nameWithOwner,
        pushedAt: .pushedAt[0:10],
        isPrivate,
        isArchived,
        description,
        releaseCount: .releases.totalCount,
        languages: [.languages.edges[] | {name: .node.name, size: .size}]
      }
  ]
}' > combined-gta/data/projects.json

echo "wrote combined-gta/data/projects.json ($(jq '.repos|length' combined-gta/data/projects.json) repos)"
```

- [ ] **Step 2: Run it and verify the output**

```bash
mkdir -p combined-gta/data scripts
chmod +x scripts/gen-projects.sh
./scripts/gen-projects.sh
jq -r '.repos[] | "\(.nameWithOwner) \(.pushedAt) rel=\(.releaseCount)"' combined-gta/data/projects.json
```

Expected: 7 repos listed. `InviteInstitute/lm-dashboard` shows `rel=1`.

- [ ] **Step 3: Write `combined-gta/data/overrides.json`**

Slot 6 is FireWatch, which has no repo. It is authored entirely here. Blurbs are drawn from the resume and repo descriptions, not invented.

```json
{
  "InviteInstitute/lm-dashboard": {
    "slot": 1,
    "slotName": "lm dashboard",
    "blurb": "Realtime learning analytics for AI tutors. Turns 41K+ live events into structured context so a tutor can catch a struggling student mid-lesson.",
    "status": "shipped"
  },
  "InviteInstitute/vex-agent-integration": {
    "slot": 2,
    "slotName": "vex agent",
    "blurb": "Connects VEX robotics triggers and episodes to a pedagogical agent, evaluating behavioral rules against live data on a 5 second cycle.",
    "status": "active"
  },
  "InviteInstitute/agent-lm-packages": {
    "slot": 3,
    "slotName": "agent lm packages",
    "blurb": "VEX log parser and learner models built for the Shan and Noah study.",
    "status": "active"
  },
  "Maharsh17/county-clustering": {
    "slot": 4,
    "slotName": "county clustering",
    "blurb": "Groups 3,128 US counties by social vulnerability and upward mobility, then tests whether the groups are real or an artifact of what was measured.",
    "status": "shipped"
  },
  "CS222-UIUC/fa25-team118-titans": {
    "slot": 5,
    "slotName": "collab editor",
    "blurb": "Google Docs style editor with sub-second multi-user sync over Yjs CRDT WebSockets. 88 commits, 10 weeks, team of 4.",
    "status": "shipped"
  },
  "firewatch": {
    "slot": 6,
    "slotName": "firewatch",
    "blurb": "Wildfire severity forecasting 52 weeks out, merging 3 federal datasets and 11K+ fire observations. Led 4 developers across 10 sprints.",
    "status": "shipped",
    "noRepo": true,
    "pushedAt": "2026-05-01"
  },
  "Maharsh17/resume-blaster": {
    "slot": 7,
    "slotName": "resume blaster",
    "blurb": "LaTeX resume tailoring driven by language models.",
    "status": "active"
  },
  "HarshithaS2023/ai4all": {
    "slot": 8,
    "slotName": "ai4all",
    "blurb": "AI4ALL coursework and project site.",
    "status": "active"
  }
}
```

- [ ] **Step 4: Verify both files are valid JSON**

```bash
jq empty combined-gta/data/projects.json && jq empty combined-gta/data/overrides.json && echo "both valid"
```

Expected: `both valid`

- [ ] **Step 5: Commit**

```bash
git add scripts/gen-projects.sh combined-gta/data/
git commit -m "Add project data generator and hand-authored overrides"
```

---

### Task 3: Save Slots Page

This is where `load game` sends everyone, so it carries the most weight.

**Files:**
- Create: `combined-gta/js/slots.js`
- Modify: `combined-gta/loadgame.html`

**Interfaces:**
- Consumes: `data/projects.json` and `data/overrides.json` from Task 2.
- Produces: `loadgame.html` containing `<ul id="save-slots">`. No later task depends on internals.

- [ ] **Step 1: Write `combined-gta/js/slots.js`**

Reuses the existing `menu-option--datagame` classes exactly, so the slots look like real GTA save files. Falls back to overrides alone if the generated file fails to load, per the spec's error handling.

```js
/* Renders project save slots into loadgame.html.
 * Slot markup mirrors the original gta-sa-menu loadgame page exactly:
 * li.menu-option.menu-option--datagame > a > span.left + span.right
 * Empty slots use menu-option--datagame-blank, same as the game. */
(function () {
	var list = document.getElementById("save-slots");
	if (!list) return;

	var SLOTS = 8;
	var MONTHS = ["JAN","FEB","MAR","APR","MAY","JUN","JUL","AUG","SEP","OCT","NOV","DEC"];

	function pad(n) { return n < 10 ? "0" + n : "" + n; }

	function fmtDate(iso) {
		if (!iso) return "";
		var p = iso.split("-");
		return pad(parseInt(p[2], 10)) + " " + MONTHS[parseInt(p[1], 10) - 1] + " " + p[0];
	}

	function esc(s) {
		return String(s == null ? "" : s)
			.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;")
			.replace(/"/g, "&quot;");
	}

	function blankSlot(i) {
		return '<li class="menu-option menu-option--datagame menu-option--datagame-blank">' +
			'<a href="#">save file ' + i + ' not present</a></li>';
	}

	function filledSlot(entry) {
		var href = entry.noRepo || entry.isPrivate
			? "#"
			: "https://github.com/" + entry.nameWithOwner;
		var right = fmtDate(entry.pushedAt) + "  " + entry.status.toUpperCase();
		return '<li class="menu-option menu-option--datagame"><a href="' + esc(href) + '"' +
			(href === "#" ? "" : ' target="_blank" rel="noopener"') +
			' title="' + esc(entry.blurb) + '">' +
			'<span class="menu-option--datagame-left">' + esc(entry.slotName) + '</span>' +
			'<span class="menu-option--datagame-right">' + esc(right) + '</span>' +
			'</a></li>';
	}

	function render(overrides, projects) {
		var byName = {};
		var i;
		if (projects && projects.repos) {
			for (i = 0; i < projects.repos.length; i++) {
				byName[projects.repos[i].nameWithOwner] = projects.repos[i];
			}
		}

		var slots = [];
		for (var key in overrides) {
			if (!Object.prototype.hasOwnProperty.call(overrides, key)) continue;
			var o = overrides[key];
			var repo = byName[key] || {};
			slots[o.slot] = {
				nameWithOwner: key,
				slotName: o.slotName,
				blurb: o.blurb,
				status: o.status,
				noRepo: !!o.noRepo,
				isPrivate: !!repo.isPrivate,
				pushedAt: repo.pushedAt || o.pushedAt || ""
			};
		}

		var html = "";
		for (i = 1; i <= SLOTS; i++) {
			html += slots[i] ? filledSlot(slots[i]) : blankSlot(i);
		}
		list.innerHTML = html;
	}

	function getJSON(url) {
		return fetch(url).then(function (r) {
			if (!r.ok) throw new Error(r.status);
			return r.json();
		});
	}

	getJSON("./data/overrides.json").then(function (overrides) {
		// Slots must render even if the generated file is missing.
		return getJSON("./data/projects.json").then(
			function (projects) { render(overrides, projects); },
			function () { render(overrides, null); }
		);
	}).catch(function () {
		list.innerHTML = '<li class="menu-option menu-option--datagame ' +
			'menu-option--datagame-blank"><a href="#">save data unavailable</a></li>';
	});
})();
```

- [ ] **Step 2: Rewrite `combined-gta/loadgame.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Maharsh Jani - Projects</title>
	<link rel="icon" type="image/png" sizes="32x32" href="./assets/menu/images/favicon-32x32.png">
	<meta name="description" content="Projects by Maharsh Jani">
	<link rel="stylesheet" href="./assets/menu/fonts/myfonts.min.css">
	<link rel="stylesheet" href="./assets/menu/css/normalize.min.css">
	<link rel="stylesheet" href="./assets/menu/css/gtasamenu.min.css">
	<link rel="stylesheet" href="./assets/notify/css/gtasa-notification.min.css">
	<link rel="stylesheet" href="./css/site.css">
</head>
<body data-section="loadgame" class="container">
	<header class="menu-header">
		<h1 class="menu-title">Load Game</h1>
		<div class="menu-subtitle">Select save file to load:</div>
		<div style="background-image: url('./assets/menu/images/back2.png');" class="menu-image"></div>
	</header>
	<main class="menu-content">
		<ul class="menu-container" id="save-slots"></ul>
	</main>
	<footer class="menu-footer">
		<span class="menu-option menu-option--back"><a href="./index.html">back</a></span>
		<p class="menu-footer-message">Every date is a real last-commit date. Hover a slot for detail.</p>
	</footer>
	<script src="./assets/menu/js/gtasamenu.min.js"></script>
	<script src="./assets/notify/js/gtasa-notification.min.js"></script>
	<script src="./js/slots.js"></script>
	<script src="./js/site.js"></script>
</body>
</html>
```

- [ ] **Step 3: Create the stylesheet this and later pages reference**

Create `combined-gta/css/site.css`. Namespaced under `site-` so it can never collide with a vendored recreation. Futura is the face the recreation already uses for non-heading text, so this introduces no new font.

```css
/* New styling only. Never edit anything under assets/. */

.site-prose {
	font-family: Futura, Arial, Helvetica, sans-serif;
	font-size: 15px;
	line-height: 1.6;
	color: #d9dcd9;
	text-transform: none;
	letter-spacing: 0.2px;
}

.site-scroll {
	overflow-y: auto;
	max-height: 100%;
	padding: 0 40px;
}
```

- [ ] **Step 4: Verify slots render**

```bash
cd combined-gta && python3 -m http.server 8899 >/dev/null 2>&1 &
sleep 2 && curl -s http://localhost:8899/loadgame.html | grep -c 'id="save-slots"'
lsof -ti :8899 | xargs kill -9
```

Expected: `1`. Then open `http://localhost:8899/loadgame.html` in a browser and confirm 8 rows appear, slot 1 reads `lm dashboard`, and the right column shows a real date plus `SHIPPED`.

- [ ] **Step 5: Commit**

```bash
git add combined-gta/js/slots.js combined-gta/loadgame.html combined-gta/css/site.css
git commit -m "Render projects as GTA save slots"
```

---

### Task 4: Skill Bars Page

**Files:**
- Create: `combined-gta/js/stats.js`
- Modify: `combined-gta/stats.html`

**Interfaces:**
- Consumes: `data/projects.json` from Task 2.
- Produces: `stats.html` containing `<section id="skill-bars">`.

- [ ] **Step 1: Write `combined-gta/js/stats.js`**

Bar fill is project coverage, not byte share. Byte share was measured and produced "94% HTML" because one collaborator repo contributes 16.69 MB of generated HTML. Coverage is outlier-resistant.

```js
/* Renders skill bars into stats.html.
 * Bar fill = fraction of projects a language appears in, NOT byte share.
 * Byte share was measured and is misleading here: a single repo contributes
 * 16.69 MB of the 16.70 MB total HTML, which would render as "94% HTML".
 * Counting projects makes one huge generated file count exactly once. */
(function () {
	var host = document.getElementById("skill-bars");
	if (!host) return;

	function esc(s) {
		return String(s == null ? "" : s)
			.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
	}

	function bar(name, count, total) {
		var pct = Math.round((count / total) * 100);
		return '<section class="gtasa-stats-bar">' +
			'<p class="gtasa-stats-bar--title">' + esc(name) +
			' (' + count + '/' + total + ')</p>' +
			'<div class="gtasa-stats-bar--progress">' +
			'<div style="width: ' + pct + '%;"></div>' +
			'</div></section>';
	}

	fetch("./data/projects.json").then(function (r) {
		if (!r.ok) throw new Error(r.status);
		return r.json();
	}).then(function (data) {
		var total = data.repos.length;
		var counts = {};
		var i, j, seen, langs;

		for (i = 0; i < data.repos.length; i++) {
			seen = {};
			langs = data.repos[i].languages || [];
			for (j = 0; j < langs.length; j++) {
				if (seen[langs[j].name]) continue;
				seen[langs[j].name] = true;
				counts[langs[j].name] = (counts[langs[j].name] || 0) + 1;
			}
		}

		var rows = [];
		for (var k in counts) {
			if (Object.prototype.hasOwnProperty.call(counts, k)) {
				rows.push({ name: k, count: counts[k] });
			}
		}
		rows.sort(function (a, b) { return b.count - a.count || a.name.localeCompare(b.name); });

		var html = "";
		for (i = 0; i < rows.length; i++) {
			html += bar(rows[i].name, rows[i].count, total);
		}
		host.innerHTML = html;
	}).catch(function () {
		host.innerHTML = '<p class="gtasa-stats-bar--title">stats unavailable</p>';
	});
})();
```

- [ ] **Step 2: Rewrite `combined-gta/stats.html`**

Keeps the vendored `gtasa-stats` classes and the original inline background so the page renders identically to the recreation.

```html
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Maharsh Jani - Stats</title>
	<link rel="icon" type="image/png" sizes="32x32" href="./assets/stats/images/favicon-32x32.png">
	<meta name="description" content="Language coverage across Maharsh Jani's projects">
	<style>
		body { background: rgb(71, 71, 71); }
	</style>
	<link rel="stylesheet" href="./assets/stats/css/normalize.min.css">
	<link rel="stylesheet" href="./assets/stats/css/styles.min.css">
	<link rel="stylesheet" href="./assets/notify/css/gtasa-notification.min.css">
</head>
<body data-section="stats">
	<div class="page-container">
		<h1 class="page-title">Stats</h1>
		<p class="page-info">Share of projects each language appears in. Not a proficiency score.</p>
	</div>

	<div class="gtasa-stats--container">
		<div class="gtasa-stats--box">
			<h2 class="gtasa-stats-title">Languages</h2>
			<section class="gtasa-stats--bars" id="skill-bars"></section>
		</div>
	</div>

	<div class="page-container">
		<p class="page-info"><a href="./index.html">back to menu</a></p>
	</div>

	<script src="./assets/notify/js/gtasa-notification.min.js"></script>
	<script src="./js/stats.js"></script>
	<script src="./js/site.js"></script>
</body>
</html>
```

- [ ] **Step 3: Verify the bars match the measured values**

```bash
cd combined-gta && python3 -m http.server 8899 >/dev/null 2>&1 &
sleep 2 && curl -s http://localhost:8899/stats.html | grep -c 'id="skill-bars"'
lsof -ti :8899 | xargs kill -9
```

Expected: `1`. Open in a browser and confirm the top bar reads `Python (5/7)`. If it reads `HTML` first, the byte-share bug has regressed.

- [ ] **Step 4: Commit**

```bash
git add combined-gta/js/stats.js combined-gta/stats.html
git commit -m "Render language coverage as GTA stat bars"
```

---

### Task 5: Mission Log

**Files:**
- Create: `combined-gta/missions.html`

**Interfaces:**
- Consumes: `css/site.css` from Task 3.
- Produces: `missions.html`. Linked from `index.html` in Task 7.

- [ ] **Step 1: Write `combined-gta/missions.html`**

All content is from the resume, condensed but not rewritten into new claims. Titles in BankGothic via `menu-title`, prose in Futura via `site-prose`, matching how the game splits its faces.

```html
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Maharsh Jani - Missions</title>
	<link rel="icon" type="image/png" sizes="32x32" href="./assets/menu/images/favicon-32x32.png">
	<meta name="description" content="Experience of Maharsh Jani, CS and Learning Sciences at UIUC">
	<link rel="stylesheet" href="./assets/menu/fonts/myfonts.min.css">
	<link rel="stylesheet" href="./assets/menu/css/normalize.min.css">
	<link rel="stylesheet" href="./assets/menu/css/gtasamenu.min.css">
	<link rel="stylesheet" href="./assets/notify/css/gtasa-notification.min.css">
	<link rel="stylesheet" href="./css/site.css">
</head>
<body data-section="missions" class="container">
	<header class="menu-header">
		<h1 class="menu-title">Missions</h1>
		<div class="menu-subtitle">Maharsh Jani - CS &amp; Learning Sciences, UIUC, expected May 2028</div>
		<div style="background-image: url('./assets/menu/images/back5.png');" class="menu-image"></div>
	</header>
	<main class="menu-content site-scroll">
		<div class="site-prose">
			<h2>NSF AI INVITE Institute - Software Engineer</h2>
			<p>Champaign, IL. Dec 2025 to present.</p>
			<ul>
				<li>Built a real-time learning analytics tool that helps AI tutors catch struggling students mid-lesson, capturing 9 live signals from student code and robot movement at sub-second latency.</li>
				<li>Shipped a live session dashboard, demoed to NSF site visit reviewers, turning 41K+ events into structured context for AI tutors.</li>
				<li>Cut database storage 84% with a normalized 5-table redesign, recovered 24 hours of silently dropped logs per query, and added a health endpoint that auto-degrades to 503.</li>
				<li>Launched an agent intervention platform letting a 10+ person team deploy tutor behaviors by config change, evaluating rules against 200K+ events on a 5 second cycle.</li>
			</ul>

			<h2>Illinois Women in CS DevAda - Lead Software Engineer</h2>
			<p>Champaign, IL. Jan 2026 to present.</p>
			<ul>
				<li>Led 4 developers across 10 weekly sprints from zero ML experience to a deployed product.</li>
				<li>Built FireWatch, forecasting wildfire severity 52 weeks out, merging 3 federal datasets and 11K+ fire observations into one live pipeline.</li>
				<li>Engineered the stack end to end: 8 model-ready features, a time series model, and an interactive Streamlit and PyDeck map.</li>
			</ul>

			<h2>Student Alumni Ambassadors - VP of Alumni Relations</h2>
			<p>Champaign, IL. Mar 2026 to present.</p>
			<ul>
				<li>Organize events hosting 250+ guests and coordinate 10+ volunteers across 7 game days for a 90-member organization.</li>
				<li>Built a 650-person alumni database with outreach templates.</li>
			</ul>

			<h2>Management Leadership for Tomorrow - Career Prep Fellow</h2>
			<p>Remote. Jan 2026 to present.</p>
			<ul>
				<li>Selected for a competitive 18-month program with partners including LinkedIn, Bloomberg, and Deloitte.</li>
			</ul>

			<h2>Education</h2>
			<p>University of Illinois Urbana-Champaign. BS in Computer Science &amp; Learning Sciences, expected May 2028. GPA 4.0.</p>
			<ul>
				<li>Chancellor's Scholar (top 1%), James Scholar, Dean's List x3, We CU Scholar.</li>
				<li>CS Student Ambassadors, Research Scholar, Peer Mentoring Center, Teach for America Ignite.</li>
			</ul>

			<p><a href="https://github.com/Maharsh17/Maharsh17.github.io/raw/main/Maharsh_Jani_Resume_.pdf">Download the full resume (PDF)</a></p>
		</div>
	</main>
	<footer class="menu-footer">
		<span class="menu-option menu-option--back"><a href="./index.html">back</a></span>
	</footer>
	<script src="./assets/menu/js/gtasamenu.min.js"></script>
	<script src="./assets/notify/js/gtasa-notification.min.js"></script>
	<script src="./js/site.js"></script>
</body>
</html>
```

- [ ] **Step 2: Extend `css/site.css` for the prose block**

Append to `combined-gta/css/site.css`:

```css
/* BankGothic for headings matches .menu-option, the face the recreation
   already uses for labels. Beckett is reserved for .menu-title. */
.site-prose h2 {
	font-family: BankGothic, sans-serif;
	font-size: 20px;
	color: rgb(194, 162, 43);
	text-transform: uppercase;
	margin: 24px 0 4px;
	letter-spacing: 1px;
}

.site-prose p { margin: 0 0 8px; }

.site-prose ul { margin: 0 0 16px; padding-left: 20px; }

.site-prose li { margin-bottom: 6px; }

.site-prose a { color: rgb(194, 162, 43); }
```

- [ ] **Step 3: Verify it renders and scrolls**

```bash
cd combined-gta && python3 -m http.server 8899 >/dev/null 2>&1 &
sleep 2 && curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8899/missions.html
lsof -ti :8899 | xargs kill -9
```

Expected: `200`. Open in a browser. `.container` sets `overflow: hidden`, so confirm the mission text scrolls inside `.site-scroll` rather than being clipped. If it clips, the `site-scroll` rule is not applying.

- [ ] **Step 4: Commit**

```bash
git add combined-gta/missions.html combined-gta/css/site.css
git commit -m "Add mission log from resume content"
```

---

### Task 6: Contact Page

**Files:**
- Modify: `combined-gta/options.html`

**Interfaces:**
- Consumes: `css/site.css`.
- Produces: `options.html`. Linked from `index.html` in Task 7.

- [ ] **Step 1: Rewrite `combined-gta/options.html`**

Phone number deliberately absent, per the global constraints.

```html
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Maharsh Jani - Contact</title>
	<link rel="icon" type="image/png" sizes="32x32" href="./assets/menu/images/favicon-32x32.png">
	<meta name="description" content="Contact Maharsh Jani">
	<link rel="stylesheet" href="./assets/menu/fonts/myfonts.min.css">
	<link rel="stylesheet" href="./assets/menu/css/normalize.min.css">
	<link rel="stylesheet" href="./assets/menu/css/gtasamenu.min.css">
	<link rel="stylesheet" href="./assets/notify/css/gtasa-notification.min.css">
</head>
<body data-section="options" class="container">
	<header class="menu-header">
		<h1 class="menu-title">Contact</h1>
		<div style="background-image: url('./assets/menu/images/back3.png');" class="menu-image"></div>
	</header>
	<main class="menu-content">
		<ul class="menu-container menu-container--options">
			<li class="menu-option"><a href="mailto:majani2@illinois.edu">email</a></li>
			<li class="menu-option"><a href="https://www.linkedin.com/in/janimaharsh/" target="_blank" rel="noopener">linkedin</a></li>
			<li class="menu-option"><a href="https://github.com/Maharsh17" target="_blank" rel="noopener">github</a></li>
			<li class="menu-option"><a href="https://www.janimaharsh.com" target="_blank" rel="noopener">website</a></li>
		</ul>
	</main>
	<footer class="menu-footer">
		<span class="menu-option menu-option--back"><a href="./index.html">back</a></span>
	</footer>
	<script src="./assets/menu/js/gtasamenu.min.js"></script>
	<script src="./assets/notify/js/gtasa-notification.min.js"></script>
	<script src="./js/site.js"></script>
</body>
</html>
```

- [ ] **Step 2: Verify no phone number leaked onto any page**

```bash
grep -rn "<phone-number>" . && echo "LEAK FOUND" || echo "clean"
```

Expected: `clean`

- [ ] **Step 3: Commit**

```bash
git add combined-gta/options.html
git commit -m "Add contact page"
```

---

### Task 7: Main Menu and Retiring Dead Pages

**Files:**
- Modify: `combined-gta/index.html`
- Delete: `combined-gta/game.html`, `deletegame.html`, `language.html`, `notifications.html`

**Interfaces:**
- Consumes: all pages from Tasks 3 through 6.
- Produces: the site's front door.

- [ ] **Step 1: Rewrite `combined-gta/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Maharsh Jani</title>
	<link rel="icon" type="image/png" sizes="32x32" href="./assets/menu/images/favicon-32x32.png">
	<meta name="description" content="Maharsh Jani - CS &amp; Learning Sciences at UIUC. Projects, experience, and contact.">
	<meta property="og:type" content="website">
	<meta property="og:title" content="Maharsh Jani">
	<link rel="stylesheet" href="./assets/menu/fonts/myfonts.min.css">
	<link rel="stylesheet" href="./assets/menu/css/normalize.min.css">
	<link rel="stylesheet" href="./assets/menu/css/gtasamenu.min.css">
	<link rel="stylesheet" href="./assets/notify/css/gtasa-notification.min.css">
</head>
<body data-section="main" class="container">
	<header class="menu-header">
		<h1 class="menu-title">Maharsh Jani</h1>
		<div style="background-image: url('./assets/menu/images/back8.png');" class="menu-image"></div>
	</header>
	<main class="menu-content">
		<ul class="menu-container">
			<li class="menu-option"><a href="./loadgame.html">load game</a></li>
			<li class="menu-option"><a href="./missions.html">missions</a></li>
			<li class="menu-option"><a href="./stats.html">stats</a></li>
			<li class="menu-option"><a href="./map.html">map</a></li>
			<li class="menu-option"><a href="./hud.html">hud</a></li>
			<li class="menu-option"><a href="./options.html">contact</a></li>
			<li class="menu-option"><a href="./quitgame.html">quit game</a></li>
		</ul>
	</main>
	<footer class="menu-footer">
		<p class="menu-footer-message">Built on fan-made GTA San Andreas web recreations. All trademarks are of their respective owners.</p>
	</footer>
	<script src="./assets/menu/js/gtasamenu.min.js"></script>
	<script src="./assets/notify/js/gtasa-notification.min.js"></script>
	<script src="./js/site.js"></script>
</body>
</html>
```

- [ ] **Step 2: Delete the retired pages**

```bash
cd /Users/maharsh/Documents/Research/gta-web/combined-gta
rm -f game.html deletegame.html language.html notifications.html
```

- [ ] **Step 3: Verify nothing still links to a deleted page**

```bash
cd /Users/maharsh/Documents/Research/gta-web/combined-gta
grep -n 'game\.html\|deletegame\.html\|language\.html\|notifications\.html' *.html \
  | grep -v 'loadgame\.html\|quitgame\.html' && echo "DANGLING LINK" || echo "no dangling links"
```

Expected: `no dangling links`. `map.html` and `quitgame.html` were ported with `back` links pointing at `index.html`, so they need no change, but if this reports a hit in `map.html`, fix that link to `./index.html`.

- [ ] **Step 4: Commit**

```bash
git add -A combined-gta/
git commit -m "Rewrite main menu and retire unused pages"
```

---

### Task 8: HUD Status and Map Relabel

**Files:**
- Modify: `combined-gta/hud.html`
- Modify: `combined-gta/map.html`
- Modify: `combined-gta/quitgame.html`

**Interfaces:**
- Consumes: nothing.
- Produces: final page set. `check.sh` passes fully after this task.

- [ ] **Step 1: Add a status line to `combined-gta/hud.html`**

The React HUD renders into `#root` and must not be touched. Add one line above it. Replace the existing back-link anchor with this block:

```html
<a href="./index.html" style="position:fixed;top:18px;left:24px;z-index:10;font-family:BankGothic,sans-serif;font-size:20px;letter-spacing:2px;color:rgb(74,90,107);text-transform:uppercase;text-decoration:none">back</a>
<p style="position:fixed;top:18px;right:24px;z-index:10;font-family:BankGothic,sans-serif;font-size:14px;letter-spacing:1px;color:rgb(74,90,107);text-transform:uppercase;margin:0;text-align:right">Currently: software engineer at the NSF AI INVITE Institute</p>
```

- [ ] **Step 2: Relabel the map**

In `combined-gta/map.html`, replace the `menu-map--message` span content:

```html
<span class="menu-map--message">champaign, illinois<br>press L for show legend</span>
```

And the city label:

```html
<span class="menu-map--city">Champaign</span>
```

- [ ] **Step 3: Point quit game somewhere real**

In `combined-gta/quitgame.html`, replace the two menu options:

```html
<li class="menu-option"><a href="./index.html">no</a></li>
<li class="menu-option"><a href="https://github.com/Maharsh17" target="_blank" rel="noopener">si</a></li>
```

- [ ] **Step 4: Run the full harness**

```bash
cd /Users/maharsh/Documents/Research/gta-web && ./check.sh
```

Expected: `ALL CHECKS PASSED`, exit 0. This is the first time it passes.

- [ ] **Step 5: Commit**

```bash
git add combined-gta/hud.html combined-gta/map.html combined-gta/quitgame.html
git commit -m "Relabel HUD, map, and quit page for personal site"
```

---

### Task 9: Deploy to GitHub Pages

**Files:**
- Modify: none locally. Publishes `combined-gta/` to `Maharsh17/Maharsh17.github.io`.

**Interfaces:**
- Consumes: a passing `check.sh` from Task 8.
- Produces: a live site.

- [ ] **Step 1: Confirm the harness passes before publishing**

```bash
cd /Users/maharsh/Documents/Research/gta-web && ./check.sh || echo "DO NOT DEPLOY"
```

Expected: `ALL CHECKS PASSED`. Do not continue otherwise.

- [ ] **Step 2: Clone the Pages repo**

```bash
cd /tmp && rm -rf pages-deploy
git clone https://github.com/Maharsh17/Maharsh17.github.io.git pages-deploy
```

- [ ] **Step 3: Copy the site in, preserving the existing resume PDF**

`missions.html` links the PDF at its current path, so it must survive.

```bash
cd /tmp/pages-deploy
test -f Maharsh_Jani_Resume_.pdf || echo "WARNING: resume PDF missing, missions.html link will 404"
find . -maxdepth 1 -mindepth 1 -not -name '.git' -not -name 'LICENSE' \
  -not -name 'Maharsh_Jani_Resume_.pdf' -exec rm -rf {} +
cp -R /Users/maharsh/Documents/Research/gta-web/combined-gta/. .
ls -1
```

Expected listing includes `index.html`, `assets/`, `data/`, `js/`, `css/`, `LICENSE`, `Maharsh_Jani_Resume_.pdf`.

- [ ] **Step 4: Verify the deploy copy serves before pushing**

```bash
cd /tmp/pages-deploy && python3 -m http.server 8899 >/dev/null 2>&1 &
sleep 2
for p in index.html loadgame.html stats.html missions.html options.html; do
  printf "%-18s %s\n" "$p" "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8899/$p)"
done
lsof -ti :8899 | xargs kill -9
```

Expected: all `200`.

- [ ] **Step 5: Push**

```bash
cd /tmp/pages-deploy
git add -A
git commit -m "Deploy GTA San Andreas themed personal site"
git push origin HEAD
```

- [ ] **Step 6: Verify live**

Wait about a minute for Pages to build, then:

```bash
curl -s -o /dev/null -w '%{http_code}\n' https://maharsh17.github.io/
curl -s https://maharsh17.github.io/ | grep -o '<title>[^<]*</title>'
```

Expected: `200` and `<title>Maharsh Jani</title>`.

- [ ] **Step 7: Commit the source repo**

```bash
cd /Users/maharsh/Documents/Research/gta-web
git add -A && git commit -m "Site complete and deployed"
```

---

## Post-Implementation Notes

Two spec questions remain open and neither blocks the build:

1. FireWatch occupies slot 6 with no outbound link. If a repo is made public, add it to `REPOS` in `scripts/gen-projects.sh` and drop the `noRepo` flag from `overrides.json`.
2. If `janimaharsh.com` should point here, add a `CNAME` file to the Pages repo and configure DNS. Nothing else changes.

To refresh project data later, rerun `./scripts/gen-projects.sh` and redeploy. Dates and language coverage update automatically. Blurbs never change unless edited by hand.
